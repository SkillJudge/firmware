config BR2_PACKAGE_ENCALLOCERTD
	bool "encalertd"
	default y
	help
	  编码器报警守护进程（原 system_monitor bash 脚本的 C 重写版）。
	  主循环 / MQTT 上报（QoS1+spool 必达）/ 报警去重由 C 控制，
	  具体恢复动作调用 /root/encoder/actions/*.sh 小脚本。
	  设计文档见 encoder/EncoderAlertdDesign.md。
