# encodermain 编码器主控 - 版本维护

> 程序：encodermain（编码器主控进程，C 语言）
> 入口：`src/main.c`
> 版本宏：`src/common.h` 中 `#define ENCM_VERSION "1.0.0"`
> 版本查询：`encodermain -V`（打印 `encodermain <版本号>`）
> 当前版本：见下方"版本历史"最新一条
> 测试报告根目录：`encoder/doc/版本维护/测试报告/`

---

## 版本规则

- 采用语义化版本 `MAJOR.MINOR.PATCH`：
  - **MAJOR**：不兼容的接口/协议变更
  - **MINOR**：向下兼容的功能新增
  - **PATCH**：向下兼容的缺陷修复
- 每次修订代码必须：
  1. 修改 `src/common.h` 中的 `ENCM_VERSION` 宏
  2. 在本文件"版本历史"中追加一条记录，描述修改内容
  3. 版本升级（MAJOR 或 MINOR 变更）需附带单元测试报告，报告存放于测试报告根目录，命名 `encodermain_vX.Y.Z_单元测试报告.md`

---

## 版本历史

### v1.0.0（2026-09-20）

**变更类型**：基线版本建立

**修改内容**：
- 确认已有版本机制：`ENCM_VERSION "1.0.0"`（`src/common.h`），`-V` 选项打印版本号
- 新增本版本维护文件 `版本维护.md`

**单元测试**：
- 报告：`encoder/doc/版本维护/测试报告/encodermain_v1.0.0_单元测试报告.md`
