# OpenIPC 固定 Wi-Fi 参数。
# 板端放置位置：/etc/wifi.conf
# 板端文件权限：600
#
# 原生 S40network 会读取这些参数并启动 wlan0。
# WIFI_DEVICE 是 OpenIPC 用于加载无线驱动的设备类型，不是 SSID。
# 必须根据实际无线芯片填写，不能留空。
# eth0 仅作为本地维护口使用，其网段必须与 Wi-Fi 网段不同。

WIFI_DEVICE='rtl8811cu-generic'
WIFI_SSID='skilljudge'
WIFI_PASSWORD='12345678'

